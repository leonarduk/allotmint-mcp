# Test dependencies only. Deliberately NOT `-r requirements.txt`: the test
# suite never touches Postgres or the embedding model (retrieval is stubbed),
# and pulling sentence-transformers in would drag PyTorch into every CI run for
# code the tests don't execute.
fastapi>=0.115
pydantic>=2.9
pydantic-ai-slim[openai]>=1.0
mcp>=1.9
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
pyyaml>=6.0
langfuse>=2.0
