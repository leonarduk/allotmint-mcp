# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "allotmint-mcp"

[setup]
script = '''
set -euxo pipefail

sudo mkdir -p /opt/java25

curl -fsSL \
  "https://api.adoptium.net/v3/binary/latest/25/ga/linux/x64/jdk/hotspot/normal/eclipse" \
  -o /tmp/temurin25.tar.gz

sudo tar \
  --extract \
  --gzip \
  --file=/tmp/temurin25.tar.gz \
  --directory=/opt/java25 \
  --strip-components=1

sudo update-alternatives \
  --install /usr/bin/java java /opt/java25/bin/java 2500

sudo update-alternatives \
  --install /usr/bin/javac javac /opt/java25/bin/javac 2500

sudo update-alternatives --set java /opt/java25/bin/java
sudo update-alternatives --set javac /opt/java25/bin/javac

echo 'export JAVA_HOME=/opt/java25' >> "$HOME/.bashrc"
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> "$HOME/.bashrc"

export JAVA_HOME=/opt/java25
export PATH="$JAVA_HOME/bin:$PATH"

java -version
javac -version

./mvnw -B dependency:go-offline'''
